module integers {

    // This is giving me a false sense of security, when am I getting pwned?
    pure def myAdd(x,y) = x + y
    pure def mySub(x,y) = x - y
    pure def myDiv(x,y) = x / y
    pure def myMult(x,y) = x * y
    pure def myPow(x,y) = x^y
    pure def myMod(x,y) = x%y
    
}